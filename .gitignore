target/
dist/
.DS_Store

# Resume: LaTeX output, and the company-tailored variants, which stay local.
resume/build/
resume/variants/
# Data for the old Python generator, kept locally for reference.
resume/json/

# Left over from the Docusaurus/Python setup; safe to delete locally.
node_modules/
.docusaurus/
/build/
resume/.venv/
resume/tex_src/

# Logo source artwork; the site uses the monogram traced into templates/.
Spelts_logo.eps
